# 4 MB two-OTA partition table.
#
# Each app slot lives entirely within one 1 MB-aligned XIP-mappable region
# (ota_0 in region 0, ota_1 in region 1), satisfying the ESP8266 rule that an
# app partition must fall within a single integrated 1 M block. The bulb id and
# rollback state live in `nvs`, outside both app slots, so they survive updates.
#
# Name,     Type, SubType, Offset,   Size
nvs,        data, nvs,     0x9000,   0x4000
otadata,    data, ota,     0xd000,   0x2000
phy_init,   data, phy,     0xf000,   0x1000
ota_0,      app,  ota_0,   0x10000,  0xF0000
ota_1,      app,  ota_1,   0x110000, 0xF0000
